import FormFieldPage from "@/playground/FormField";

export const metadata = { title: "Form field — Playground" };

export default function Page() {
  return (
    <div className="mx-auto w-full max-w-5xl p-6 md:p-10">
      <FormFieldPage />
    </div>
  );
}
