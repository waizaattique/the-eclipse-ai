import PagePlaceholder from "@/components/PagePlaceholder";

export default function TimetablePage() {
  return (
    <PagePlaceholder
      title="Timetable"
      description="See every class, deadline, and study block for the week in one place."
    />
  );
}
