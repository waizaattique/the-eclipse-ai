import PagePlaceholder from "@/components/PagePlaceholder";

export default function RemindersPage() {
  return (
    <PagePlaceholder
      title="Reminders"
      description="Never miss a submission, exam, or renewal again."
    />
  );
}
