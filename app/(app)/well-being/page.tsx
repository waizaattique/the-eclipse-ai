import PagePlaceholder from "@/components/PagePlaceholder";

export default function WellBeingPage() {
  return (
    <PagePlaceholder
      title="Well-being"
      description="Check in on your workload and stress level before it piles up."
    />
  );
}
